<UserControl x:Class="EncryptionWindowApplication.UserControls.EncryptionMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EncryptionWindowApplication.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="1.2*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock" Margin="10" FontSize="40" FontFamily="Verdana"
            Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" HorizontalAlignment="Center"></TextBlock>
        
        <Button FontFamily="Verdana" BorderBrush="Black" BorderThickness="1" FontSize="30"
            Background="White" Margin="5" Grid.Row="5" Grid.Column="3"
            Padding="5" x:Name="CancelButton">Cancel</Button>

        <Button FontFamily="Verdana" BorderBrush="Black" BorderThickness="1" FontSize="30"
            Background="White" Margin="5" Grid.Row="5" Grid.Column="0"
            Padding="5" x:Name="ConfirmButton">Confirm</Button>

        <TextBlock x:Name="TimerTextBlock" Grid.Row="2" Grid.ColumnSpan="6" Grid.Column="0"
            FontFamily="Verdana" FontSize="35" HorizontalAlignment="Center" VerticalAlignment="Center"
            Grid.RowSpan="2"></TextBlock>

        <ProgressBar Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" Maximum="100" Height="40"
            x:Name="Progress"></ProgressBar>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4"
            HorizontalAlignment="Center">
            
            <TextBlock FontSize="35" Margin="0 0 5 0">Key:</TextBlock>
            <PasswordBox FontSize="35" Width="350" x:Name="KeyPasswordBox"></PasswordBox>
        </StackPanel>
    </Grid>
</UserControl>
