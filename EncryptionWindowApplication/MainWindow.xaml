<Window x:Class="EncryptionWindowApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EncryptionWindowApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="4.5*" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Grid.Column="1" Click="BrowseButton" Margin="5 10 5 10"
                Style="{StaticResource Button}">Browse</Button>

            <Button Grid.Row="1" Grid.Column="1" Click="UpdateButton"
                Style="{StaticResource Button}" VerticalAlignment="Bottom"
                Margin="5 0 5 0" Padding="0 10 0 10">Update</Button>
            
            <Grid Grid.Column="0" Grid.Row="2" x:Name="StackPanelGrid" AllowDrop="True">
                <Border BorderBrush="Black" BorderThickness="1" x:Name="StackPanelBorder">
                    <ScrollViewer>
                        <StackPanel x:Name="RecentFilesStackPanel"></StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.6*"></RowDefinition>
                    <RowDefinition Height="0.4*"></RowDefinition>
                </Grid.RowDefinitions>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" x:Name="FileNameTextBlock"
                    FontSize="35"></TextBlock>

                <TextBlock Grid.Row="1" x:Name="PathTextBlock"
                    FontSize="20"></TextBlock>

                <Button FontFamily="Verdana" BorderBrush="DarkRed" Foreground="DarkRed" Grid.Row="1"
                    Grid.Column="1" BorderThickness="1" Background="White"
                    Margin="0 2.5 5 5" FontSize="15" Click="DeleteActiveFile">Delete</Button>
                
                <Button x:Name="EncryptButton" FontFamily="Verdana" Background="White" Grid.Column="1"
                    Grid.Row="0" BorderBrush="Black" Margin="0 0 5 2.5" BorderThickness="1"
                    FontSize="20">Encrypt</Button>
            </Grid>
        </Grid>
    </Border>
</Window>
